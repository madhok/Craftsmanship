#include <iostream>

using namespace std;

void swap(int &a, int &b)
{
    int temp;
    temp = a;
    a = b;
    b = temp;
}
void SelectionSort(int* unsortedArray, int arrlength)
{
    if(arrlength == 0 || arralength == 1)
    {
        return;
    }    
   for(int i = 1; i < arrlength; i++)
   {
        if(unsorted[i] < insorted[i-1])
        {
            unsorted
        }
   }
}



int main()
{
    
}