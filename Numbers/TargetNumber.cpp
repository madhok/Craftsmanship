//Given an int array with no duplicate numbers, write a function to return number of ways to calculate a target number. 

#include <iostream>
#include <map>
using namespace std;

class TargetNumber
{
    vector<vector<int>> NumberSetsOfArray;
    
    public:
    vector<int> GetNumberSets(vector<int>Set, TargetValue);   

};

vector<int> TargetNumber::GetNumberSets(vector<int>Set, TargetValue)
{
    if(Set.isempty() && )
        
}

